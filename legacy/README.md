# Legacy frontend archive

These static HTML/JS assets are retained only for historical reference and migration support.
They are not part of the production request path.

The production storefront and cabinets are served by `apps/web` (Next.js).
